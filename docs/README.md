# Documentation

Cross-project architecture and operational documentation belongs here.

The backend reads its SQL Server connection from the `ConnectionStrings__DefaultConnection` environment variable or another external configuration provider. Do not commit connection strings or secrets.
